import React from "react"

/* TODO
1. Convert to class
2. In componentDidMount, check localStorage
3. If localStorage doesn't exist, create
4. Render if sessionStorage is false
5. Hide if sessionStorage is true
*/
export default () => <div>Indication</div>
